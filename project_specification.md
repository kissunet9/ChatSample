# 1:1 실시간 채팅 서비스 프로젝트 개요 및 세부 기능 명세서 (v1.1)

---

## 1. 프로젝트 개요 (Project Overview)

### 1.1 프로젝트 정의 및 목적
본 프로젝트는 별도의 회원가입이나 로그인(인증) 절차 없이, 사용자의 닉네임과 채팅방 이름을 입력하여 생성/참여할 수 있는 **웹 기반 1:1 실시간 채팅 서비스(ChatSample)**입니다. 

### 1.2 핵심 특징 및 변경사항
- **Firebase 프로젝트 연동**: 프로젝트명 **`chatsample`** (Firebase Realtime Database) 기반 연동
- **디자인 테마**: 세련되고 깔끔한 **라이트 모드 (Light Mode)** 테마 적용
- **채팅방 이름 필수 설정**: 채팅방 생성 시 방 제목(채팅방 이름) 및 작성자 닉네임을 모두 입력받도록 강화
- **인증 없는 간편 접속**: 회원가입/로그인 없이 닉네임 설정만으로 즉시 대화 가능
- **1:1 정원 관리**: 각 채팅방은 최대 2명까지만 입장 가능 (2/2명 정원 초과 시 입장 제한)
- **실시간 대화 구현**: Firebase Realtime Database를 활용한 초저지연 실시간 메시지 송수신
- **자동 데이터 정리 (Clean-up)**: 모든 참여자가 퇴장하여 인원 수가 0명이 된 채팅방은 데이터베이스 및 목록에서 자동으로 즉시 삭제

### 1.3 기술 스택 (Tech Stack)
- **Frontend**: Standard Web Technologies (HTML5, CSS3 Vanilla Light Theme, JavaScript ES6+)
- **Backend / Database**: Firebase Realtime Database (Project: `chatsample`)
- **Design System**: Responsive Light Mode with CSS Variables, Soft Shadows, Smooth Animations
- **Hosting / Environment**: Web Browser Client-side Application

---

## 2. 세부 기능 명세서 (Detailed Feature Specification)

| 기능 ID | 페이지 | 기능 분류 | 기능 상세 | 테스트 시나리오 |
| :--- | :--- | :--- | :--- | :--- |
| **FEAT-01** | 채팅리스트 | 채팅방 목록 조회 (라이트 모드 UI) | - Firebase Realtime Database (`chatsample` 프로젝트)의 `/rooms` 데이터를 실시간 수신하여 생성된 채팅방 목록을 카드 형태로 라이트 모드 디자인에 맞춰 노출한다.<br>- 각 채팅방 카드에 **채팅방 이름**, **생성자 닉네임**, **현재 참여 인원 수(예: 1/2명, 2/2명)** 및 **입장 가능 상태 태그**를 표기한다. | 1. 메인 화면 접속 시 라이트 모드 UI와 생성된 채팅방 목록이 정상 노출되는지 확인한다.<br>2. 타 사용자가 방 생성/퇴장 시 목록 및 인원 수가 실시간으로 갱신되는지 확인한다. |
| **FEAT-02** | 채팅리스트 | 채팅방 만들기 | - '채팅방 만들기' 버튼 클릭 시 **채팅방 이름(방 제목)**과 **사용할 닉네임**을 모두 입력받는 다이얼로그(라이트 모드 모달)를 출력한다.<br>- '확인' 클릭 시 입력값 유효성 검증(공백 체크 등) 후 Firebase에 신규 채팅방을 생성하고 생성된 채팅방으로 즉시 이동한다. | 1. '만들기' 버튼 클릭 시 채팅방 이름 및 닉네임 입력 모달이 출력되는지 확인한다.<br>2. 채팅방 이름과 닉네임 입력 후 '확인' 클릭 시 신규 방이 생성되고 해당 채팅방으로 이동하는지 확인한다.<br>3. 채팅방 이름이나 닉네임 중 하나라도 빈 값일 경우 경고 메시지가 띄워지고 방 생성이 차단되는지 확인한다. |
| **FEAT-03** | 채팅리스트 | 채팅방 참여 | - 여유 자리가 있는 방(현재 인원 < 2명) 클릭 시 해당 방에서 사용할 닉네임 입력 다이얼로그를 출력한다.<br>- '참여' 버튼 클릭 시 인원수를 1 증가(2/2)시키고 해당 채팅방 페이지로 이동한다.<br>- 꽉 찬 방(2/2명) 클릭 시 '정원 초과' 안내 알림을 노출한다. | 1. 1/2명 상태인 방 클릭 후 닉네임 입력 및 '참여' 클릭 시 정상 입장되는지 확인한다.<br>2. 입장 후 해당 방의 인원수가 2/2로 실시간 반영되는지 확인한다.<br>3. 2/2명인 방 클릭 시 정원 초과 안내 팝업이 노출되고 입장이 제한되는지 확인한다. |
| **FEAT-04** | 채팅방 | 실시간 채팅 | - 대화 입력창에 메시지 작성 후 전송(엔터 키 또는 버튼) 시 Firebase `/rooms/{roomId}/messages` 경로에 메시지를 추가한다.<br>- 라이트 모드 말풍선 스타일로 본인 메시지(우측 브랜드 컬러 말풍선), 상대방 메시지(좌측 밝은 그레이 말풍선 + 닉네임)를 구별하여 실시간 출력한다. | 1. 메시지 작성 후 전송 시 내 화면 우측에 라이트 모드 말풍선 형태로 즉시 노출되는지 확인한다.<br>2. 상대방 화면 좌측에 닉네임과 함께 메시지가 실시간 수신되는지 확인한다.<br>3. 메시지 수신 시 스크롤이 대화창 최하단으로 자동 이동하는지 확인한다. |
| **FEAT-05** | 채팅방 | 채팅 나가기 | - '나가기' 버튼 클릭 시 해당 방 참여 인원수를 1 감소시키고 채팅리스트 페이지로 복귀한다.<br>- 참여 인원이 0명이 되는 경우 해당 채팅방 노드를 Firebase에서 자동 삭제한다.<br>- 브라우저 종료/새로고침(beforeunload) 시에도 퇴장 처리 로직이 수행되도록 구현한다. | 1. 2명이 있는 방에서 1명이 '나가기'를 할 경우 리스트로 이동하고 인원이 1/2로 감소하는지 확인한다.<br>2. 남은 1명이 '나가기'를 할 경우 방 데이터가 자동 삭제되어 리스트에서 사라지는지 확인한다.<br>3. 브라우저 탭 닫기/새로고침 시에도 퇴장 처리가 정상 반영되는지 확인한다. |

---

## 3. 데이터 구조 설계 (Firebase Realtime Database Schema)

```json
{
  "rooms": {
    "{roomId}": {
      "roomId": "string",
      "title": "string",       // 채팅방 이름 (필수 입력 항목)
      "creator": "string",     // 채팅방 생성자 닉네임
      "memberCount": 1,        // 현재 참여 인원 수 (1 or 2)
      "createdAt": 1710000000000,
      "messages": {
        "{messageId}": {
          "sender": "string",
          "content": "string",
          "timestamp": 1710000005000
        }
      }
    }
  }
}
```
