# 1:1 실시간 채팅 서비스 프로젝트 개요 및 세부 기능 명세서

---

## 1. 프로젝트 개요 (Project Overview)

### 1.1 프로젝트 정의 및 목적
본 프로젝트는 별도의 회원가입이나 로그인(인증) 절차 없이, 사용자 닉네임 입력만으로 쉽고 빠르게 1:1 대화를 나눌 수 있는 **웹 기반 1:1 실시간 채팅 서비스(ChatSample)**입니다. 

### 1.2 핵심 특징
- **인증 없는 간편 접속**: 회원가입/로그인 없이 닉네임 설정만으로 이용 가능
- **1:1 정원 관리**: 각 채팅방은 최대 2명까지만 입장 가능 (정원 초과 시 입장 제한)
- **실시간 대화 구현**: Firebase Realtime Database를 활용한 초저지연 실시간 메시지 송수신
- **자동 데이터 정리 (Clean-up)**: 모든 참여자가 퇴장하여 인원 수가 0명이 된 채팅방은 데이터베이스 및 목록에서 자동으로 즉시 삭제

### 1.3 기술 스택 (Tech Stack)
- **Frontend**: Standard Web Technologies (HTML5, CSS3, JavaScript ES6+)
- **Backend / Database**: Firebase Realtime Database
- **Hosting / Environment**: Web Browser Client-side Application

---

## 2. 세부 기능 명세서 (Detailed Feature Specification)

| 기능 ID | 페이지 | 기능 분류 | 기능 상세 | 테스트 시나리오 |
| :--- | :--- | :--- | :--- | :--- |
| **FEAT-01** | 채팅리스트 | 채팅방 목록 조회 | - Firebase Realtime Database의 `/rooms` 데이터를 실시간으로 수신하여 현재 생성된 채팅방 목록을 구성한다.<br>- 각 채팅방 항목에 방 제목, 생성자 닉네임, 현재 참여 인원 수(예: 1/2, 2/2)를 표기한다. | 1. 메인 화면 접속 시 현재 생성되어 있는 채팅방 목록이 정상 노출되는지 확인한다.<br>2. 타 사용자가 방을 생성/퇴장할 때 목록 및 인원 수가 실시간으로 갱신되는지 확인한다. |
| **FEAT-02** | 채팅리스트 | 채팅방 만들기 | - '만들기' 버튼 클릭 시 대화에 사용할 닉네임과 방 제목을 입력받는 다이얼로그(모달)를 띄운다.<br>- '확인' 클릭 시 입력 값 유효성 검사 후 Firebase에 신규 채팅방을 생성하고, 생성된 채팅방으로 즉시 이동한다. | 1. '만들기' 버튼을 눌렀을 때 닉네임 및 방 제목 입력 모달이 정상 표시되는지 확인한다.<br>2. 닉네임 입력 후 '확인' 클릭 시 신규 방이 생성되고 해당 채팅방 페이지로 이동하는지 확인한다.<br>3. 공백 또는 빈 값 입력 시 경고 메시지가 띄워지고 방 생성이 차단되는지 확인한다. |
| **FEAT-03** | 채팅리스트 | 채팅방 참여 | - 여유 자리가 있는 방(현재 인원 < 2명) 클릭 시 해당 채팅방에서 사용할 닉네임 입력 다이얼로그를 표시한다.<br>- '참여' 버튼 클릭 시 해당 방의 인원 수를 1 증가시키고 채팅방 페이지로 이동한다.<br>- 이미 정원이 찬 방(2/2명) 클릭 시 참여 불가 안내 팝업을 출력한다. | 1. 1/2명 상태인 방을 클릭하여 닉네임 입력 후 '참여' 버튼 클릭 시 정상 입장되는지 확인한다.<br>2. 입장 후 해당 방의 인원 상태가 2/2로 갱신되는지 확인한다.<br>3. 2/2명으로 꽉 찬 방 클릭 시 정원 초과 알림 메시지가 노출되고 입장이 제한되는지 확인한다. |
| **FEAT-04** | 채팅방 | 실시간 채팅 | - 대화 입력창에 메시지 작성 후 전송(엔터 키 또는 버튼) 시 Firebase `/rooms/{roomId}/messages` 경로에 메시지를 저장한다.<br>- 작성자 메시지는 우측 말풍선, 상대방 메시지는 좌측 말풍선(작성자 닉네임 포함)으로 구분하여 실시간 출력한다. | 1. 메시지 입력 후 전송 시 내 화면 우측에 말풍선 형태로 즉시 노출되는지 확인한다.<br>2. 상대방 화면 좌측에 닉네임과 함께 메시지가 실시간으로 도착하여 표시되는지 확인한다.<br>3. 새 메시지 수신 시 대화창 스크롤이 자동으로 최하단으로 이동하는지 확인한다. |
| **FEAT-05** | 채팅방 | 채팅 나가기 | - '나가기' 버튼 클릭 시 해당 채팅방 참여 인원 수를 1 감소시키고 채팅리스트 페이지로 복귀한다.<br>- 방 참여 인원이 0명이 되는 경우 해당 채팅방 노드를 Firebase에서 자동 삭제한다.<br>- 브라우저 종료/새로고침 이벤트(beforeunload) 시에도 퇴장 처리 로직이 동작하도록 구현한다. | 1. 2명이 있는 방에서 1명이 '나가기'를 수행할 때 리스트로 이동하고 인원이 1/2로 줄어드는지 확인한다.<br>2. 1명이 남은 방에서 마저 '나가기'를 수행할 때 방이 삭제되어 리스트에서 사라지는지 확인한다.<br>3. 채팅 중 브라우저 탭을 닫거나 새로고침 시 퇴장 처리가 정상 반영되는지 확인한다. |

---

## 3. 데이터 구조 설계 (Firebase Realtime Database Schema)

```json
{
  "rooms": {
    "{roomId}": {
      "roomId": "string",
      "title": "string",
      "creator": "string",
      "memberCount": 1, // 1 or 2
      "createdAt": 1710000000000,
      "messages": {
        "{messageId}": {
          "sender": "string",
          "content": "string",
          "timestamp": 1710000005000
        }
      }
    }
  }
}
```
